[project]
name = "schema-validator"
version = "0.1.0"
description = "Flexible data schema validation with custom rules engine"
requires-python = ">=3.8"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"
